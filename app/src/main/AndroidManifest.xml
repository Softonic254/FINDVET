<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.user.locvet">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />

    <application
        android:name="android.support.multidex.MultiDexApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:logo="@drawable/examine"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.Request"></activity>
        <activity android:name=".activities.VetPassReset" />
        <activity android:name=".activities.PasswordReset" />
        <activity android:name=".activities.VetChatActivity" />
        <activity android:name=".activities.farmer_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.CreateActivity" />
        <activity android:name=".activities.FarmerActivity" />
        <activity android:name=".activities.vetLogin" />
        <activity android:name=".activities.vetsignup" />
        <activity android:name=".activities.vetactivity" />
    </application>

</manifest>